1. Location awareness {
   New visitor IP: get location via ip-api and store in redis cache
   Existing IP: {
    if user is logged in, get location from user profile
    if user is not logged in, get location from redis cache
   }
}
2. Geography awareness (Content Localization to country & currency){
  All content are displayed to user based on location, i.e currency, timezone, etc
  Contents are localized based on country e.g Users in Nigeria see only contents in Nigeria
  No currency conversion, users see prices in their local currency
}
3. Push Notification integration (FCM) to notify users of new messages, offers, orders, etc.{
  Setup Pusher
  Setup Reverb
  Setup Broadcasting
  Setup Notifications{

  }
}
4. Subscription plans, jobs, queues, redis cache{
  Setup Job {
    SubscriptionExpiringJob,
    SubscriptionExpiredJob,
    SubscriptionAutoRenewedJob
  }
  Setup Queues{
    For Notifications
    For Jobs
    
  }
  Setup Redis Cache
}
5. Payment & escrow integrations, payouts, refunds, returns, replacements, disputes{
  Setup Gateway{
    Paystack
    Flutterwave
  }
  Setup Jobs{
    ConfirmPaymentJob,
    RefundPaymentJob,
    ReleasePaymentJob,
    RetryPaymentJob
  }
  Setup Webhooks{
    PaystackWebhookHandler,
    FlutterwaveWebhookHandler
  }
}
6. Database Schema Migrations & Models Definitions{
  Setup Database Migrations
  Setup Models
  Setup Relationships
}
7. Seeding (Categories, Brands, Models, Items, Roles, Permissions, Users)
8. UI/UX Completion, with Light, Dark and System mode
9. Authentication (Registration, Login, Forgot Password, Reset Password)
10. CICD/Deployment, Docker, Testing, etc
11. AWS Setup, etc
12. Application Logic Completion{
  Setup Jobs{
    AbandonedCartJob
  }
  Setup Exports{
    Invoices
  }
}
13. Admin Interfaces